export const metadataMap: Record<string, { title: string; desc: string }> = {
  Offline: { title: process.env.NEXT_PUBLIC_CASINO_NAME || "", desc: "" },
};

export const chatSupportEnabled = [""];

export const allowOTP = [""];

export const allowPaymentGateway = [""];
